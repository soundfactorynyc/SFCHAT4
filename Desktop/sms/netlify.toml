[build]
  publish = "."
  functions = "netlify/functions"
  command = "echo 'No build needed - static files only'"

[functions]
  node_bundler = "esbuild"

# Clean API paths → Functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Force /admin → landing/login
[[redirects]]
  from = "/admin"
  to = "/widget/"
  status = 302
  force = true
